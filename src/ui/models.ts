
export enum IconType {
    Expand = `expand`,
    Collapse = `collapse`,
    Json = `json`,
    List = `list`,
    CaretRight = `caret-right`,
    CaretDown = `caret-down`,
}

export interface VariableGroup {
    collapsed: boolean;
    comingSoon: boolean;
  }
  